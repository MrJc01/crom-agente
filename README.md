# Teste de Conectividade com Daemon

Este é um script para testar a conectividade com um daemon.

## Como usar:

1.  **Instale as dependências:**
    ```bash
    pip install -r requirements.txt
    ```
2.  **Execute o script:**
    ```bash
    python test_daemon_connection.py [opções]
    ```

## Opções:

(A ser definido após as clarificações)

